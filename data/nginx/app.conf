server {
    listen 80;
    server_name kna.dital.nu;

    location / {
        return 301 https://$host$request_uri;
    }

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
}

server {
    listen 443 ssl;
    server_name kna.dital.nu;
    location / {
        proxy_pass http://dital.nu; 
    }
    ssl_certificate /etc/letsencrypt/live/dital.nu/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/dital.nu/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}